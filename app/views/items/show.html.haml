
%h2= @item.name
%h3 Note:
%p= @item.note
%p= @item.url

= link_to 'Edit', edit_item_path(@item), :class=>'btn'
= link_to 'Back', items_path, :class=>'btn'
= link_to 'Destroy', @item, :confirm => 'Are you sure?', :method => :delete, :class=>'btn'
